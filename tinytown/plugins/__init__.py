"""Per-site hooks. A plugin is named in sites/<site>/site.json and may define
extra_sources(request), landmarks(site, paths), outline(site, request, overrides, paths),
and refine_building(building). See docs/ARCHITECTURE.md."""
