# Hurlbut Church entrance correction

Building `820059527`, sanctuary `+u` façade. OBB, mapped footprint, orientation, volume shells, roof, windows, parapet, pilasters, side door and materials are preserved.

The user screenshot shows doors pushed directly into the Gothic windows and stairs floating above the ground. The old door specified `y: 1.7` and `steps: 10`; `doorEl` adds a second 1.8 m step rise, placing the actual threshold at 3.5 m and door head at 5.8 m. The Gothic glazing begins at 5.1 m, so this overlaps by 0.7 m even before trim.

Local references copied to this folder: `reference-1.jpg` is geolocated Street View from Pratt Avenue, matching the sanctuary +u front; `reference-6.jpg` is Chautauqua Institution's public photo, https://www.chq.org/wp-content/uploads/2022/06/HurlbutChurch.jpg . Both show a broad masonry stair, a real top landing and a clear wall band between doors and windows. Exact centimetre dimensions cannot be inferred from these photos; revised proportions use the existing building scale and measured modeled terrain.

The revised explicit door sill is 1.05 m (2.45 m lower), with unchanged 2.3 m door height. Head is 3.35 m and nominal gap to the unchanged glazing is 1.75 m. Built-in `steps` are removed. A 6.6 m wide, 1.34 m deep solid landing meets the threshold; an adjacent 3.3 m long stair has eleven solid treads, 1.915 m total rise, and dark side handrails as in the photographs. The highest tread is 5 mm below the landing, preventing coplanar overlap at their joint. The landing embeds to -1.35 m and stairs to -1.37 m relative to the production base.

`measure-ground.mjs` substitutes the final authoring blueprint into the production site in memory, executes the exact production terrain/grading/seating code, and returns samples just after building base assignment. It makes no production source or aggregate-site mutations. `before-ground-measurement.json` is the original, and `ground-measurement.json` is the final. Lowering the door does add the +u entrance to `blueprintFrontages`, but final base is unchanged: -2.824929284622291. Model minimum terrain is -3.174599723815919. At the stair toe u=23.5, terrain varies from about -0.75 to -0.86 m relative to the base. Grids preserve signed heights and sample records include unshifted site coordinates.

Integration: apply `integration-patch.json` to the matching building blueprint and overrides blueprint only, then rebuild scene assets. There are no global renderer edits, footprint edits, or changes to shared aggregate files in this work.

Validation completed: blueprint lint 0 errors/0 warnings; preservation check confirmed every field outside front door and details is exactly equal to baseline. The before, after and baked-stream renders cover front, oblique, contact and entry views. Streaming follows production typed-array packing and material shader restoration. Eleven solid steps embed at least 0.531 m below sampled terrain; the first exposed riser varies 0.062–0.159 m across 25 toe samples. The top tread is 0.005 m below the landing, avoiding coplanar overlapping floor faces. The lowered door is visibly clear of the unchanged Gothic windows and occludes the belt course where they intersect. The stream round-trip changes bounds by only 0.000001081 m. All geometry is finite and no browser errors occurred. See `validation.json` and `geometry-report.json`.
